interface formatCodecList {
    mp4: availableCodec[]
    mov: availableCodec[]
}
interface availableCodec {
    label: string
}

const  formatCodecList = {
    mp4:[]
}

export default formatCodecList